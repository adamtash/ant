# Memory

Persistent knowledge base for ant.

> NOTE: `MEMORY.md` lives in `workspaceDir`. If your `workspaceDir` is set to this repo, this file is active. If your `workspaceDir` is `~/.ant`, your active memory is `~/.ant/MEMORY.md`. See `docs/agent-files.md`.
