# ANT CLI Environment Variables
# Copy this file to .env and fill in your values

# -----------------------------------------------------------------------------
# Config selection (optional)
# -----------------------------------------------------------------------------
# Use this if you want `ant` to always load a specific config without `-c`.
# ANT_CONFIG_PATH=~/.ant/ant.config.json

# -----------------------------------------------------------------------------
# Secrets (recommended to keep in .env, not ant.config.json)
# -----------------------------------------------------------------------------
# OpenAI API Key (for OpenAI provider)
OPENAI_API_KEY=

# LM Studio Base URL (for local models)
LMSTUDIO_BASE_URL=http://localhost:1234/v1

# Claude API Key (if using Claude CLI)
CLAUDE_API_KEY=

# GitHub Copilot Token (if using Copilot CLI)
COPILOT_TOKEN=

# -----------------------------------------------------------------------------
# ant overrides (optional) - these override ant.config.json when set
# -----------------------------------------------------------------------------
# ANT_WORKSPACE_DIR=/path/to/ant-cli
# ANT_STATE_DIR=~/.ant
# ANT_UI_STATIC_DIR=/path/to/ant-cli/ui/dist
# ANT_RUNTIME_REPO_ROOT=/path/to/ant-cli
# ANT_UI_PORT=5117
# ANT_UI_HOST=127.0.0.1
# ANT_GATEWAY_PORT=18789
# ANT_GATEWAY_HOST=127.0.0.1
# ANT_LOG_LEVEL=info
# ANT_LOG_FILE_LEVEL=trace
# ANT_LOG_FILE_PATH=.ant/ant.log

# -----------------------------------------------------------------------------
# Telegram overrides (optional)
# -----------------------------------------------------------------------------
# ANT_TELEGRAM_ENABLED=true
# ANT_TELEGRAM_BOT_TOKEN=
# ANT_TELEGRAM_MODE=polling
# ANT_TELEGRAM_WEBHOOK_PUBLIC_URL=
# ANT_TELEGRAM_WEBHOOK_PATH=
# ANT_TELEGRAM_WEBHOOK_SECRET_TOKEN=
