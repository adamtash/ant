# Status

Last update: 2026-01-27

- Worktree at `/Users/a/Projects/ant` on branch `ant-main`.
- New CLI lives under `ant/` and includes core runtime, memory, WhatsApp, subagents, and CLI commands.
- Config template: `ant/ant.config.json`.
- Core modules: `ant/src/runtime/*`, `ant/src/memory/*`, `ant/src/whatsapp/*`.

Next: install deps (`pnpm install`) and run `pnpm dev -- run -c ant.config.json`.
